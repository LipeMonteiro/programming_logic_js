// Ask for a number and tell if it is positive, negative or zero.

function classifyNumber(number) {
    if (number > 0) return "Positive";
    if (number < 0) return "Negative"
    return "Zero"
}

console.log(classifyNumber('-10'))        